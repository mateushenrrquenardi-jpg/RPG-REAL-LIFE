{
  "name": "rpg-real-life",
  "version": "2.0.0",
  "private": true,
  "description": "RPG da Vida Real — front-end estático com Supabase.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node --check app.js && node --check db.js && node --check js/constants.js && node --check js/rules.js",
    "test": "node --test test/*.test.cjs",
    "verify": "npm run check && npm test"
  }
}
